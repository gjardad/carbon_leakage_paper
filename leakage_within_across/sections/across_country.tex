\textbf{[To be written in step 3: full migration of the CMdG-replication evidence from \texttt{IMPORT\_LEAKAGE.md}, structured as: (i) replication specification, (ii) headline Belgium results, (iii) four-spec robustness, (iv) Belgium-vs-France comparison.]}

We replicate \citet{CosterMejeanDiGiovanni2024}'s identification of EU ETS-driven cross-border leakage on Belgian customs imports 2000--2019. The headline finding mirrors theirs in specification but inverts in sign: where the French panel returns Phase~3 coefficients consistent with a $\sim$12 percentage-point reallocation of imports toward non-ETS source countries on regulated products, the Belgian panel returns $-0.0024$~(s.e.\ $0.0012$, $p < 0.05$) on the same outcome under the preferred fixed-effect structure --- two orders of magnitude smaller than the French estimate and pointing in the opposite direction. The Phase~2 coefficient is $-0.0037$~(***), again wrong-signed for leakage. Robustness across four control-group specifications (Specs A--D from \texttt{IMPORT\_LEAKAGE.md}) and six fixed-effect columns is consistent with this null-or-negative pattern, with no specification recovering CMdG-style positive coefficients.

\textit{Section to be expanded with: (a) full Eq.~(1) specification and four-cell control structure, (b) Table~1 analog reproducing CMdG Table~1 columns~(1)--(6) for both share and probability outcomes, (c) Figure~3 analog (event-study leads and lags), (d) Belgium-vs-France comparison table, (e) interpretation paragraph linking the Belgian negative significance to candidate mechanisms (intra-EU substitution, chemicals saturation, sample-size attenuation in col~(5)).}
